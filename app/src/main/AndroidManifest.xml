<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androapplite.vpn3">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.android.vending.BILLING" />
    <uses-permission android:name="android.permission.GET_TASKS" />

    <application
        android:name="com.androapplite.shadowsocks.ShadowsocksApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/facebook_app_id"/>

        <!-- 参数修改片段 -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="588708daf43e48200f0014d9" />
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="Google" />

        <!-- 完全覆盖区-Start -->
        <receiver android:name="com.smartads.plugin.MonitorReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <activity android:name="com.smartads.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"/>

        <activity
            android:name="com.facebook.ads.InterstitialAdActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@android:style/Theme.Translucent" />
        <activity
            android:name="com.google.android.gms.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize"
            android:theme="@android:style/Theme.Translucent" />

        <activity
            android:name="com.vungle.publisher.FullScreenAdActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />

        <activity
            android:name="com.androapplite.shadowsocks.activity.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar.FullScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.androapplite.shadowsocks.activity.NewUserGuideActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar.FullScreen" />

        <service
            android:name="yyf.shadowsocks.service.ShadowsocksVpnService"
            android:permission="android.permission.BIND_VPN_SERVICE"
            android:process=":vpn">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>
        <!-- Enable background dispatching to improve delivery on non-Google Play devices -->
        <receiver
            android:name="com.google.android.gms.analytics.AnalyticsReceiver"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.google.android.gms.analytics.ANALYTICS_DISPATCH" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.google.android.gms.analytics.AnalyticsService"
            android:enabled="true"
            android:exported="false" />

        <!-- Enable Google Play Store Campaign reports -->
        <receiver
            android:name="com.google.android.gms.analytics.CampaignTrackingReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.vending.INSTALL_REFERRER" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.google.android.gms.analytics.CampaignTrackingService"
            android:enabled="true" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="52a14ce59287e0edc5512692515ab0270feaef99" />

        <receiver android:name="com.androapplite.shadowsocks.broadcast.ShadowsocksVPNServiceReceiver">
            <intent-filter>
                <action android:name="${applicationId}.INIT" />
                <action android:name="${applicationId}.CONNECTING" />
                <action android:name="${applicationId}.CONNECTED" />
                <action android:name="${applicationId}.STOPPING" />
                <action android:name="${applicationId}.STOPPED" />
                <action android:name="${applicationId}.ERROR" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.androapplite.shadowsocks.activity.ConnectivityActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />

        <service android:name="com.androapplite.shadowsocks.service.ServerListFetcherService" />
        <service
            android:name="com.androapplite.shadowsocks.service.ConnectionTestService"
            android:exported="false" />

        <activity
            android:name="com.androapplite.shadowsocks.activity.ServerListActivity"
            android:label="@string/server_list" />
        <activity android:name="com.androapplite.shadowsocks.activity.ShareActivity" />
        <activity
            android:name="com.facebook.FacebookActivity"
            android:configChanges="keyboard|keyboardHidden|screenLayout|screenSize|orientation"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!--<provider-->
            <!--android:name="com.facebook.FacebookContentProvider"-->
            <!--android:authorities="com.facebook.app.FacebookContentProvider${applicationId}"-->
            <!--android:exported="true" />-->

        <activity android:name="com.androapplite.shadowsocks.activity.SettingsActivity"
            android:theme="@style/AppTheme.NoActionBar"
            />

        <service
            android:name="com.androapplite.shadowsocks.service.TimeCountDownService"
            android:enabled="true"
            android:exported="false" />

        <activity android:name="com.androapplite.shadowsocks.activity.VIPActivity" />
        <activity
            android:name="com.androapplite.shadowsocks.activity.WatchVideoADDialogActivity"
            android:theme="@style/Theme.MyDialog">

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <receiver
            android:name="com.androapplite.shadowsocks.broadcast.WatchVideoADCallbackReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="${applicationId}.VIDEO_AD_FINISH" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.androapplite.shadowsocks.activity.CommonAlertActivity"
            android:theme="@style/Theme.MyDialog"
            android:exported="true"
            >

            <!-- <intent-filter> -->
            <!-- <action android:name="android.intent.action.MAIN" /> -->


            <!-- <category android:name="android.intent.category.LAUNCHER" /> -->
            <!-- </intent-filter> -->
        </activity>

        <receiver
            android:name="com.androapplite.shadowsocks.broadcast.WIFIDetectReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.net.wifi.supplicant.STATE_CHANGE" />
            </intent-filter>
        </receiver>

        <activity
            android:name="com.androapplite.shadowsocks.activity.CheckInActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <receiver
            android:name="com.androapplite.shadowsocks.broadcast.CheckInAlarmReceiver"
            android:enabled="true"
            android:exported="false">

            <!-- <intent-filter> -->
            <!-- <action android:name="${applicationId}.CHECK_IN_ALARM" /> -->
            <!-- </intent-filter> -->
        </receiver>
        <receiver
            android:name="com.androapplite.shadowsocks.broadcast.BootReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.androapplite.shadowsocks.service.AppCheckService"
            android:enabled="true"
            android:exported="false"></service>
    </application>

</manifest>